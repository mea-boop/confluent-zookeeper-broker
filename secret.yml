---
# ==============================================================================
# ZooKeeper — Secrets (Ansible Vault AES256)
# Encrypt: ansible-vault encrypt roles/vars/secret.yml
# ==============================================================================

# Kafka admin user credentials
kafka_admin_username: "admin"
kafka_admin_password: "admin-secret"

# Artifactory credentials
artifactory_user: "your-artifactory-user"
artifactory_password: "your-artifactory-password"
